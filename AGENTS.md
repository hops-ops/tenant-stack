# AGENTS

- Avoid Upbound-hosted configuration packages (paid-account restrictions). Favor crossplane-contrib packages.
- Target Crossplane 2+: do not set deletionPolicy on managed resources; use managementPolicies and defaults.
